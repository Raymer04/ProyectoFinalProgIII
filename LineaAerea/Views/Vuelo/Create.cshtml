@model LineaAerea.Models.Vuelo

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm())
{
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Vuelo</legend>

        <div class="editor-field">
            @Html.DropDownList("Id", (IEnumerable<SelectListItem>)ViewBag.Procedencias)
            @Html.ValidationMessageFor(model => model.Procedencia.Lugar)
        </div>

          <div class="editor-label">
            @Html.LabelFor(model => model.Procedencia.Lugar, "Destino")
        </div>
        <div class="editor-field">
            @Html.DropDownList("Id", (IEnumerable<SelectListItem>)ViewBag.Destinos)
            @Html.ValidationMessageFor(model => model.Destino.Lugar)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Salida)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Salida)
            @Html.ValidationMessageFor(model => model.Salida)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Llegada)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Llegada)
            @Html.ValidationMessageFor(model => model.Llegada)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index", null, new { @class = "button" })
</div>

